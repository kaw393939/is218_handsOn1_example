[pytest]
# Make app importable with both `pytest` and `python -m pytest`.
pythonpath = .
# Discover tests in tests/ using the test_ naming convention.
testpaths = tests
python_files = test_*.py
python_functions = test_*
